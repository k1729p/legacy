USE kp_seam01;

CREATE TABLE hibernate_sequence (
	next_val bigint(20) DEFAULT NULL
) ENGINE=InnoDB;

insert into hibernate_sequence (next_val) values (10);

CREATE TABLE revinfo (
	REV int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
	REVTSTMP bigint(20) DEFAULT NULL
) ENGINE=InnoDB;

SELECT TABLE_NAME, COLUMN_NAME, DATA_TYPE, IS_NULLABLE, COLUMN_TYPE, COLUMN_KEY, EXTRA
FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_SCHEMA = 'kp_seam01'
ORDER BY TABLE_NAME, COLUMN_NAME;
