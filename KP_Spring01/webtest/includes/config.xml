<config
    host="${webtest.host}"
    port="${webtest.port}"
    protocol="${webtest.protocol}"
    basepath="${webtest.basepath}"
    >
    <option name="ThrowExceptionOnScriptError" value="true"/>
    <header
        name="User-Agent"
        value="Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1; SV1; InfoPath.1; .NET CLR 1.1.4322)"/>
</config>
